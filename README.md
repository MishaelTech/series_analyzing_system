# Series Analyzing System with NLP
In thiis project i analyze a series with NLP and LLMs. I also scrape our own dataset from the web, use zero shot classifier, 
built an LLM text classifier, use NER to build a character network ad built a character chatbot with a character. 
For displaying the content i made use of streamlit and gradio web GUI.
